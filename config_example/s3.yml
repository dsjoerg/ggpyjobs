test:
  replays:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: YET_ANOTHER_UNIQUE_BUCKET_NAME
    s3_host_alias:
  matchblobs:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: YET_ANOTHER_UNIQUE_BUCKET_NAME
    s3_host_alias:
  minimaps:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: YET_ANOTHER_UNIQUE_BUCKET_NAME
    s3_host_alias:


# You only need to fill in the following section if you are running
# ggpyjobs locally (as opposed to just testing it)

development: &development
  replays:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: A_UNIQUE_BUCKET_NAME
    s3_host_alias:
  matchblobs:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: ANOTHER_UNIQUE_BUCKET_NAME
    s3_host_alias:
  minimaps:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: YET_ANOTHER_UNIQUE_BUCKET_NAME
    s3_host_alias:
  s2gs:
    access_key_id: YOUR_ACCESS_KEY_ID
    secret_access_key: YOUR_SECRET_ACCESS_KEY
    endpoint: s3.amazonaws.com
    bucket: YET_ANOTHER_UNIQUE_BUCKET_NAME
    s3_host_alias:
